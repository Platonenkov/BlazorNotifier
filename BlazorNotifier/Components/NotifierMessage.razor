@using BlazorNotifier.Classes
@using System.Timers
<div id="@Message.Id" class="notification__message message--@Message.Type" @onclick="Click" style="@(IsVisible ? "transform: translateX(0%); opacity: 1; visibility: visible;":null)">
    <h1>@Message.Type</h1>
    <p>@Message.Title</p>
    <button aria-labelledby="button-dismiss-@Message.Id" @onclick="Close">
        <span id="button-dismiss-@Message.Id" hidden> Dismiss</span >
        <svg viewBox="0 0 100 100" width="10" height="10">
            <g stroke="currentColor" stroke-width="6" stroke-linecap="round" stroke-linejoin="round" fill="none">
                <g transform="translate(50 50) rotate(45)"><path d="M 0 -30 v 60 z M -30 0 h 60"></path></g>
            </g>
        </svg>
    </button >
</div>
